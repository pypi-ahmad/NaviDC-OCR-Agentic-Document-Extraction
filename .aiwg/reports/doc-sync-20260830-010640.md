# Documentation sync report

- Direction: full, bidirectional reconciliation
- Scope: complete repository documentation against current application, worker,
  configuration, tests, and launcher
- Artifact-root resolution: `aiwg artifacts path` was unavailable; no
  `AIWG_ARTIFACTS_PATH` or `.aiwg-location` redirect was present, so the verified
  project-default `.aiwg/` root was used with user confirmation to audit all
- Audit lanes: product/user workflow; provider/configuration/API

## Findings resolved

1. Documented Auto layout selection and the 300/400 DPI quality pipeline.
2. Corrected claims that PDFs preserve their original render resolution.
3. Distinguished local OCR from optional online structured extraction.
4. Documented required OpenAI environment variables and transmitted data boundaries.
5. Added the provider `render_dpi` request and `max_render_dpi` health contracts.
6. Added optional structured artifacts and current manifest metadata.
7. Updated architecture and developer module maps for quality, schemas, grounding,
   validation, semantic extraction, data models, and agentic tests.
8. Updated tutorial tabs, OCR-first schema-optional behavior, and result invalidation.
9. Documented launcher-driven worker restart, model warm-up, and harmless connection reset.
10. Corrected the dark theme description and removed stale large-range warning claims.

## Auto-fixes applied

- `README.md`
- `docs/architecture.md`
- `docs/configuration-and-artifacts.md`
- `docs/developer-guide.md`
- `docs/documentation-coverage.md`
- `docs/faq.md`
- `docs/how-to-improve-accuracy.md`
- `docs/openapi.yaml`
- `docs/operations-runbook.md`
- `docs/provider-api.md`
- `docs/tutorial-first-extraction.md`

## Human-review items

None. All changes were grounded directly in current source or configuration.

## Validation

- `git diff --check`
- Relative Markdown-link resolution
- `uvx --from pyyaml python -c "..."` (OpenAPI parse and endpoint presence)
- `uv run pytest -q`
- `uv run ruff check streamlit_app.py src tests`
- `uv run ty check`
